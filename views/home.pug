extends layouts/main.pug
include mixins/videoBlock.pug

block content
    .videos
        each item in videos
            +videoBlock({
                videoFile : item.videoFile,
                title : item.title,
                views : item.views
            })